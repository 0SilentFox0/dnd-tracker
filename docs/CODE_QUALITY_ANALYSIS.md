# Аналіз якості коду D&D Combat Tracker

## Загальна оцінка

**Структура проекту:** ⭐⭐⭐⭐ (4/5) - Добре організована, з чітким поділом на модулі  
**Повторюваність коду:** ⭐⭐⭐ (3/5) - Є значне дублювання в API routes та формах  
**TypeScript використання:** ⭐⭐⭐⭐ (4/5) - Хороша типізація, але є місця для покращення  
**Компонентна архітектура:** ⭐⭐⭐⭐ (4/5) - Хороше розділення, але багато повторюваних паттернів  

---

## 🔴 Критичні проблеми

### 1. Масове дублювання авторизації в API Routes

**Проблема:** Код авторизації повторюється в **38+ API route файлах**

```typescript
// Повторюється в кожному API route:
const supabase = await createClient();
const { data: { user: authUser } } = await supabase.auth.getUser();
if (!authUser) {
  return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
}
const userId = authUser.id;
const campaign = await prisma.campaign.findUnique({
  where: { id },
  include: { members: { where: { userId } } },
});
if (!campaign || campaign.members[0]?.role !== "dm") {
  return NextResponse.json({ error: "Forbidden" }, { status: 403 });
}
```

**Вплив:** 
- ~300+ рядків дублювання
- Складно підтримувати - зміни потрібно робити в 38+ місцях
- Ризик неконсистентної логіки авторизації

**Рішення:** Створити middleware або helper функції:
```typescript
// lib/api-helpers/auth.ts
export async function requireDM(campaignId: string, userId: string)
export async function requireAuth()
export async function checkCampaignAccess(campaignId: string, userId: string)
```

---

### 2. Дублювання структури форм

**Проблема:** Повторюється структура форм у багатьох компонентах

**Приклади:**
- `RaceEditForm.tsx` і `MainSkillEditForm.tsx` мають схожу структуру
- `SkillCreateForm.tsx` та `ArtifactCreateForm.tsx` використовують однакові паттерни

**Повторювані елементи:**
```typescript
// Повторюється в кожній формі:
<Card>
  <CardHeader>
    <CardTitle>...</CardTitle>
    <CardDescription>...</CardDescription>
  </CardHeader>
  <CardContent>
    <form onSubmit={handleSubmit} className="space-y-6">
      <div className="space-y-2">
        <Label>...</Label>
        <Input ... />
      </div>
      <Button type="submit">Зберегти</Button>
    </form>
  </CardContent>
</Card>
```

**Вплив:**
- ~50+ рядків повторюваного HTML у кожній формі
- Складно уніфікувати стилі та поведінку

**Рішення:** Створити абстракцію форми:
```typescript
<FormCard title="..." description="..." onSubmit={...}>
  <FormField label="..." />
  <FormSubmitButton />
</FormCard>
```

---

### 3. Непослідовність у перевірках доступу

**Проблема:** Різні варіанти перевірки доступу в різних routes

**Приклади:**
```typescript
// Варіант 1 (більшість routes):
if (!campaign || campaign.members[0]?.role !== "dm")

// Варіант 2 (units):
if (!campaign || campaign.members.length === 0)

// Варіант 3 (characters GET):
if (!isDM && !isOwner)
```

**Вплив:** Ризик помилок безпеки, складна підтримка

---

## 🟡 Середні проблеми

### 4. Дублювання PageHeader компонентів

**Проблема:** Схожі, але різні компоненти для заголовків сторінок

**Файли:**
- `MainSkillsPageHeader.tsx`
- `RacesPageHeader.tsx`  
- `SpellsPageHeader.tsx`
- `UnitsPageHeader.tsx`

**Аналіз:**
- `MainSkillsPageHeader` - простий (37 рядків)
- `UnitsPageHeader` - складніший (90 рядків) з логікою груп
- Різна структура для схожих потреб

**Рішення:** Створити універсальний `PageHeader` з опціями:
```typescript
<PageHeader 
  title="..." 
  description="..." 
  actions={<Button>...</Button>}
  stats={...}
/>
```

---

### 5. Повторювані паттерни Card + Accordion

**Проблема:** Структура `Card > Accordion > AccordionItem` повторюється

**Приклади:**
- `app/campaigns/[id]/dm/characters/new/page.tsx` - 8 AccordionItem з однаковою структурою
- `app/campaigns/[id]/dm/characters/[characterId]/page.tsx` - аналогічно

**Статистика:**
- ~116 використань Card компонентів
- Повторювана структура в ~10+ файлах

---

### 6. Неконсистентна обробка помилок

**Проблема:** Різні підходи до обробки помилок

**Приклади:**
```typescript
// Варіант 1:
catch (error) {
  console.error("Error:", error);
  return NextResponse.json({ error: "Internal server error" }, { status: 500 });
}

// Варіант 2:
catch (error) {
  if (error instanceof z.ZodError) {
    return NextResponse.json({ error: error.issues }, { status: 400 });
  }
  return NextResponse.json({ error: "Failed to..." }, { status: 500 });
}

// Варіант 3:
catch (err) {
  console.error("Error:", err);
  alert("Помилка..."); // На фронтенді
}
```

---

## 🟢 Незначні проблеми / Покращення

### 7. Дублювання типів та інтерфейсів

**Приклади:**
- Спільні поля у `CharacterFormData` та `UnitFormData`
- Схожі типи для форм (мають загальні поля `name`, `description`, `icon`)

**Рішення:** Створити базові типи:
```typescript
interface BaseEntityFormData {
  name: string;
  description?: string;
  icon?: string;
}
```

---

### 8. Магічні числа та рядки

**Приклади:**
```typescript
// Замість константи:
if (character.level >= campaign.maxLevel) // maxLevel може бути різним
Math.floor(remainingSlots / 3) // Чому 3?
specialCount % 2 === 1 ? 4 : 5 // Магічна логіка
```

---

### 9. Великі компоненти

**Файли з великою кількістю рядків:**
- `components/races/RaceEditForm.tsx` - ~389 рядків
- `components/skills/SkillCreateForm.tsx` - потрібна перевірка
- `components/units/UnitGroupAccordion.tsx` - потрібна перевірка

**Рішення:** Розділити на менші компоненти за функціональністю

---

## 📊 Статистика

### Повторюваність коду

| Паттерн | Кількість використань | Файлів | Оцінка дублювання |
|---------|----------------------|--------|-------------------|
| Авторизація в API | ~291 | 38 | 🔴 Високе |
| Card структура | ~116 | 14 | 🟡 Середнє |
| Form структура | ~15+ | 5+ | 🟡 Середнє |
| PageHeader | 4 | 4 | 🟢 Низьке |
| AccordionItem | ~20+ | 3+ | 🟡 Середнє |

### Розміри компонентів

- Середній розмір компонента: ~150-250 рядків
- Великі компоненти (>300 рядків): ~5-8 файлів
- Маленькі компоненти (<100 рядків): більшість UI компонентів ✅

---

## ✅ Позитивні моменти

1. **Добра організація структури:** Чіткий поділ на `components/`, `lib/`, `app/`
2. **Використання TypeScript:** Хороша типізація у більшості місць
3. **Компонентний підхід:** UI компоненти добре розділені
4. **Константи винесені:** Багато констант в `lib/constants/`
5. **Кастомні хуки:** Використання `use*` хуків для логіки
6. **Zod валідація:** Використання Zod для валідації схем

---

## 🎯 Пріоритетні рекомендації

### Високий пріоритет

1. **Створити auth helpers для API routes** 
   - Економія: ~300+ рядків коду
   - Покращення підтримки та безпеки

2. **Уніфікувати структуру форм**
   - Економія: ~200+ рядків коду  
   - Краща консистентність UX

3. **Створити універсальний PageHeader**
   - Економія: ~100+ рядків коду
   - Простіша підтримка

### Середній пріоритет

4. Стандартизувати обробку помилок
5. Створити базові типи для форм
6. Винести магічні значення в константи

### Низький пріоритет

7. Розділити великі компоненти
8. Покращити документацію типів
9. Додати unit тести для критичних функцій

---

## 📝 Висновок

**Загальна оцінка:** ⭐⭐⭐ (3.5/5)

Проект має **добру структуру** та **використовує сучасні практики**, але **значно страждає від дублювання коду**, особливо в API routes та формах.

**Основні проблеми:**
- 🔴 Високе дублювання авторизації (~300+ рядків)
- 🟡 Середнє дублювання форм (~200+ рядків)
- 🟡 Неконсистентна обробка помилок

**Потенційна економія:** ~600-800 рядків коду можна винести в перевикористовувані компоненти/helpers.

**Рекомендація:** Почати з рефакторингу авторизації в API routes - це дасть найбільший ефект з найменшими зусиллями.
