/**
 * Константи для імунітетів
 */

// Список загальних імунітетів, які можуть бути у рас/юнітів
export const COMMON_IMMUNITIES = [
  "вогню",
  "вогня",
  "контролю",
  "отруєння",
  "сповільнення",
  "страху",
  "чарівництва",
  "магії",
] as const;

export type CommonImmunity = typeof COMMON_IMMUNITIES[number];

// Патерни для парсингу імунітетів з опису
export const IMMUNITY_PATTERNS = [
  /імунітет до ([^,\.;]+)/gi,
  /імунітет на ([^,\.;]+)/gi,
  /імунітет проти ([^,\.;]+)/gi,
  /імунітет\s+([^,\.;]+)/gi, // "імунітет вогонь"
] as const;
